APP_NAME="Restaurant E-commerce System"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration
# Đổi các giá trị sau theo cấu hình MySQL của bạn
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=restaurant_db
DB_USERNAME=root
DB_PASSWORD=

# Broadcasting, Cache, Queue, Session
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DRIVER=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=database
SESSION_LIFETIME=120

# Memcached
MEMCACHED_HOST=127.0.0.1

# Redis (tùy chọn - không bắt buộc)
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail Configuration
# Để test local, dùng 'log' để ghi email vào file log
# Để gửi email thật, dùng 'smtp' hoặc 'mailgun'
MAIL_MAILER=log
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@restaurant.com"
MAIL_FROM_NAME="${APP_NAME}"

# Mailgun (nếu sử dụng Mailgun)
MAILGUN_DOMAIN=
MAILGUN_SECRET=
MAILGUN_ENDPOINT=api.mailgun.net

# Postmark (nếu sử dụng Postmark)
POSTMARK_TOKEN=

# AWS Configuration (nếu sử dụng AWS S3, SES)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher (nếu sử dụng real-time broadcasting)
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# SSLCommerz Payment Gateway Configuration
# Để test: dùng sandbox credentials
# Để production: đổi sang live credentials và API_DOMAIN_URL
PROJECT_PATH=http://localhost:8000
API_DOMAIN_URL=https://sandbox.sslcommerz.com
STORE_ID=your_store_id_here
STORE_PASSWORD=your_store_password_here
IS_LOCALHOST=true

# Jetstream Features
# Đã bật sẵn: profile photos, API, teams
# Xem thêm trong config/jetstream.php


